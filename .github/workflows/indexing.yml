name: Google Indexing API (Final Fix)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  indexing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Google Indexing Script
        run: npm install -g google-indexing-script

      - name: Create Service Account Key File
        run: |
          # 작은따옴표로 감싸서 JSON 내부의 특수문자 보존
          echo '${{ secrets.INDEXING_SERVICE_ACCOUNT }}' > service_account.json

      - name: Generate URL List and Request Indexing
        run: |
          DOMAIN="https://hyungjinhan.github.io"

          # sitemap에서 URL 추출 (한글 인코딩된 상태 유지)
          curl -s "$DOMAIN/sitemap.xml" | \
          grep -oP 'https://hyungjinhan\.github\.io/[^\s<]*' | \
          sed 's/<\/loc>//g' | \
          sort -u | head -n 200 > urls.txt

          echo "✅ 총 $(wc -l < urls.txt) 개의 URL을 발견했습니다."

          # 환경 변수 설정
          export GOOGLE_APPLICATION_CREDENTIALS=$(pwd)/service_account.json

          while IFS= read -r url; do
            clean_url=$(echo "$url" | xargs)
            if [ -n "$clean_url" ]; then
              echo "🚀 요청 중: $clean_url"
              # npx 대신 전역 설치된 라이브러리 직접 호출하여 속도와 인증 안정성 확보
              google-indexing-script "$clean_url" || echo "⚠️ 실패: $clean_url"
            fi
          done < urls.txt
