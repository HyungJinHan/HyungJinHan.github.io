name: Google Indexing API (Ultimate Fix)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  indexing:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Create Service Account Key File
        run: |
          echo '${{ secrets.INDEXING_SERVICE_ACCOUNT }}' > service_account.json

      - name: Generate URL List and Request Indexing
        run: |
          # 1. 사이트맵에서 URL 추출
          DOMAIN="https://hyungjinhan.github.io"
          curl -s "$DOMAIN/sitemap.xml" | grep -oP 'https://hyungjinhan\.github\.io/[^< \n\r\t]*' | sort -u | head -n 200 > urls.txt

          echo "총 $(wc -l < urls.txt) 개의 URL을 발견했습니다."

          # 2. 개별 URL 전송 (라이브러리 버그 방지를 위해 루프 사용)
          # 인코딩된 상태 그대로 전송합니다.
          while IFS= read -r url; do
            clean_url=$(echo "$url" | xargs)
            if [ -n "$clean_url" ]; then
              echo "🚀 요청 중: $clean_url"
              # 라이브러리가 파일을 인식 못하는 문제를 방지하기 위해 URL을 직접 인자로 넘깁니다.
              npx google-indexing-script "$clean_url" || echo "⚠️ 실패: $clean_url"
            fi
          done < urls.txt
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ./service_account.json
